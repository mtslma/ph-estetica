import Link from "next/link";

export default function Servicos() {
    return (
        <main className="flex items-center w-screen h-screen flex-col justify-center">
            SERVIÇOS
            <Link href={"/"}>Página inicial</Link>
        </main>
    );
}
